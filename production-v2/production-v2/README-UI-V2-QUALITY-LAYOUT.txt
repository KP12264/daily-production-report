Production V2 — UI V2 Quality Layout Rebuild

This package rebuilds the page layout to follow the current Quality Dashboard
design direction: centered max-width layout, rounded navy navigation bar,
navy page hero, light gray background, white cards, blue active controls,
dark navy text, muted blue-gray secondary text, and clean tables.

Important:
- No sidebar is used, so menu text cannot be hidden by content overlap.
- Master Setup IDs and JavaScript behavior are preserved.
- Existing Firebase data remains unchanged.
- Step 4 Shift & Time logic remains present.
- No re-initialize is required after uploading this UI update.
